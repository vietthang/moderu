{
  "name": "moderu",
  "version": "0.0.4",
  "description": "Type-safe library for knex query & model.",
  "main": "build/src/index.js",
  "scripts": {
    "compile": "tsc",
    "bundle": "dts-bundle --name moderu --main build/src/index.d.ts",
    "test": "NODE_ENV=test mocha --recursive --compilers ts:ts-node/register --bail",
    "cover": "NODE_ENV=test nyc npm test",
    "build": "npm run compile && npm run bundle",
    "clean": "rm -rf build",
    "prepublish": "npm run clean && npm run build"
  },
  "homepage": "https://github.com/vietthang/moderu",
  "author": "Yoshi",
  "license": "MIT",
  "devDependencies": {
    "@types/bluebird": "^3.0.36",
    "@types/chai": "^3.4.34",
    "@types/lodash": "^4.14.48",
    "@types/mocha": "^2.2.32",
    "@types/node": "0.0.3",
    "chai": "^3.5.0",
    "dts-bundle": "^0.6.1",
    "istanbul": "^0.4.5",
    "mocha": "^3.1.2",
    "nyc": "^10.0.0",
    "source-map-support": "^0.4.8",
    "ts-node": "^2.0.0",
    "tslint": "^3.15.1",
    "tslint-microsoft-contrib": "^2.0.13",
    "typescript": "^2.1.4"
  },
  "dependencies": {
    "lodash": "^4.17.4",
    "sukima": "^0.4.2"
  },
  "nyc": {
    "include": [
      "src/*.ts",
      "src/**/*.ts"
    ],
    "exclude": [
      "**/*.d.ts"
    ],
    "extension": [
      ".ts"
    ],
    "require": [
      "ts-node/register"
    ],
    "reporter": [
      "json",
      "html"
    ],
    "all": true
  },
  "typings": "build/src/moderu.d.ts"
}
